import { createContext } from "react";

const StoreContext = createContext();

export default StoreContext;

// NOTES (SEC 8):
// Creating the Store Context to pass the store value down the entire application.